version: "3"

services:
  website:
    build:
      context: ./site
      dockerfile: Dockerfile
    ports:
      - "2000:3000"
    volumes:
      - ./site/src/:/boss/src/
      - ./site/public/:/boss/public/
    restart: unless-stopped