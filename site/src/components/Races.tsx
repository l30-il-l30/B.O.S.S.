import React from "react";
import { Element } from "react-scroll";

const Races: React.FC = () => {
    return <Element id="races" name="races" className="w-full h-screen" style={{ scrollSnapAlign: "center" }}>
        races
    </Element>
};

export default Races;