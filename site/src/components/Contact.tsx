import React from "react";
import { Element } from "react-scroll";

const Contact: React.FC = () => {
    return <Element id="contact" name="contact" className="w-full h-screen" style={{ scrollSnapAlign: "center" }}>
        contact
    </Element>
};

export default Contact;